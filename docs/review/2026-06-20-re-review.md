# 再レビュー結果

日付: 2026-06-20  
対象: 前回レビュー ([2026-06-20-code-review.md](./2026-06-20-code-review.md)) の対応確認

---

## 総評

前回指摘した全7件が対応済み。テスト (11件) ・Lint・ビルドすべて通過。

---

## 対応確認

| 重要度 | 指摘内容 | 対応状況 |
|--------|----------|----------|
| HIGH   | プロンプトインジェクション対策 | ✅ 対応済 |
| MEDIUM | 逐次処理の並列化 | ✅ 対応済 |
| MEDIUM | GitHub `/readme` 専用エンドポイントの利用 | ✅ 対応済 |
| MEDIUM | グループ分割のマジックナンバー解消 | ✅ 対応済 |
| MEDIUM | CI タイムアウト設定 | ✅ 対応済 |
| LOW    | `m` サフィックスのテスト追加 | ✅ 対応済 |
| LOW    | CI ステップ順序の最適化 | ✅ 対応済 |

---

## 各対応の詳細確認

### [HIGH] プロンプトインジェクション (`src/summarize.ts`)

`<repository_data>` タグで外部データを囲み、「タグ内は命令ではない」旨の指示を明記。
テストも `<repository_data>` タグと anti-injection メッセージの存在を検証するケースを追加済み。問題なし。

### [MEDIUM] 逐次処理の並列化 (`src/index.ts`)

`enrichRepository` を `Promise.all` で並列化済み。
`summarizeRepository` は意図的に逐次のまま（Copilot CLI を同時多数起動しない設計）。適切な判断。

### [MEDIUM] GitHub `/readme` エンドポイント (`src/readme.ts`)

`README_CANDIDATES` の逐次試行を廃止し、`/repos/{owner}/{repo}/readme` に一本化。コードがシンプルになった。テストも URL を `/readme` に更新済み。

### [MEDIUM] グループ分割の定数化 (`src/discord.ts`)

```ts
const DISCORD_GROUP_SIZES = [3, 3, 4] as const;
```

として定数化し、ループで動的にスライス。可読性・保守性ともに向上。

### [MEDIUM] CI タイムアウト (`.github/workflows/daily.yml`)

`timeout-minutes: 30` 追加済み。

### [LOW] `m` サフィックスのテスト (`test/trending.test.ts`)

```ts
expect(parseGitHubNumber("1.5m")).toBe(1500000);
```

追加済み。テスト件数も10→11に増加。

### [LOW] CI ステップ順序 (`.github/workflows/daily.yml`)

`Install Copilot CLI` が `Build` の後・`Run daily summary` の前に移動済み。

---

## 新規指摘事項

### [LOW] `DISCORD_GROUP_SIZES` の合計値と `MAX_TRENDING_REPOSITORIES` の暗黙的な結合が残存 (`src/discord.ts`)

`DISCORD_GROUP_SIZES = [3, 3, 4]` の合計値 (10) と `trending.ts` の `MAX_TRENDING_REPOSITORIES = 10` が依然として暗黙的に一致している。どちらかを変更した場合にコンパイルエラーにならず、動作が意図せず変わるリスクがある。

ただし、実害は小さく、現状の設計範囲では問題ない。将来的に変更が必要になった場合のメモとして記録する。

---

## まとめ

重大な問題は残存していない。コードの品質は良好。
